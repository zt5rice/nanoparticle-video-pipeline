-r requirements.txt
-e .
pytest>=7.4
# Pinned for deterministic CI lint results across environments.
ruff==0.16.4
matplotlib>=3.7
# FastAPI TestClient dependency (starlette.testclient requires httpx).
httpx
